# Specification

## Summary
**Goal:** Publish/release the current website version so the latest already-implemented changes (including GIF media updates) are deployed.

**Planned changes:**
- Trigger a new build from the current codebase using the existing publishing flow.
- Deploy the build to the preview/production environment without additional code changes.
- Verify the deployed site loads successfully and key pages render without runtime errors.

**User-visible outcome:** The live/preview site reflects the latest version, including the updated Home/Hero and Services GIF usage, and works without regressions.
